import 'package:flutter/material.dart';

class{{name.pascalCase()}}2Widget extends StatelessWidget {
  const{{name.pascalCase()}}2Widget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(child: const Text("{{name.pascalCase()}}2"));
  }
}
